import styled from 'styled-components'; 

export const Button = styled.div` 
    position: fixed; 
    width: 100%; 
    left: 50%; 
    bottom: 47px; 
    height: 10px; 
    font-size: 2.5rem; 
    z-index: 1;
    cursor: pointer; 
    color: #3a5168; 
    `